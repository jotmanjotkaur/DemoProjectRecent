package Auto1;

import org.apache.poi.openxml4j.exceptions.InvalidFormatException;
import org.example.ReadingDataFromExcel;

import java.io.IOException;
import java.util.List;

public class Teat2 {
    public static void main(String[] args) throws IOException, InvalidFormatException {

/*
        ReadingDataFromExcel read = new ReadingDataFromExcel();

        List<String> excelValues=  read.getValuesForColumn(1);
        List<String> excelValues=  read.getValuesForColumn(2);
        List<String> excelValues=  read.getValuesForColumn(3);

      /*  List<String> excelValues= null;
        try {
            excelValues = read.getValuesForColumn(1);
        } catch (InvalidFormatException e) {
            throw new RuntimeException(e);
        } catch (IOException e) {
            throw new RuntimeException(e);
        }

        printValues(excelValues);

        excelValues =read.getValuesForColumn(2);

        printValues(excelValues);

        excelValues =read.getValuesForColumn(3);

        printValues(excelValues);

       */

 /*       private static void  printValues(List<String> excelValues)
        {
            System.out.println("column : " +colIndex);
            for (String string:excelValues
                 )

            {
                System.out.println(string);

            }

        }

  */

    }
}


